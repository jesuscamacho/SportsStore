export class Product {

    constructor(
        public id?:number,
        public name?: string,
        public category?: string,
        public desciption?: string,
        public price?: number) { }
}

/* Notes:
    - ? are optional parameters
*/